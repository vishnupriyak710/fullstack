```mermaid
sequenceDiagram
    participant browser
    participant server

    browser->>server: GET /spa
    server-->>browser: HTML

    browser->>server: GET /spa.js
    server-->>browser: JavaScript

    browser->>server: GET /data.json
    server-->>browser: Notes (JSON)

    Note right of browser: Page updates without reload
